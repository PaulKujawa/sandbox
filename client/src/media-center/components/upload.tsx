import { Box } from "@mui/material";
import React from "react";

export const Upload = () => {
  return (
    <Box
      sx={{
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      file upload component
    </Box>
  );
};
